Designed to deploy as a separate webapp.
Sample JSPs currently in /templates/jsp (in the deployed webapp)
Other main interface is via /*.ws items which are mapped to Struts actions, which forward to Velocity templates that render XML
